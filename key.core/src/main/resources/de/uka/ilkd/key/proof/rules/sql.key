\sorts {
  TCustomer;
  TAccount;
}

\functions {
  TCustomer customer(int,int);
  TAccount account(int);

  int cus_id(TCustomer);
  int cus_xyz(TCustomer);
  int acc_value(TAccount);
}

\rules {
  customer_id {
    \schemaVar \term int t1, t2;
    \find( cus_id(customer(t1, t2)) )
    \replacewith(t1)
    \heuristics(simpliy)
 };

 // ...
 
}